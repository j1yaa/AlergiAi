rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY TEST MODE - ALLOWS ALL AUTHENTICATED USERS
    // ⚠️ CHANGE TO SECURE RULES AFTER DEMO
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}